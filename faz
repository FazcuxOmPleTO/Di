
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "Rota",
   Icon = 0, -- Icon in Topbar. Can use Lucide Icons (string) or Roblox Image (number). 0 to use no icon (default).
   LoadingTitle = "Carregando...",
   LoadingSubtitle = "Oi sou lyrics!",
   Theme = "DarkBlue", -- Check https://docs.sirius.menu/rayfield/configuration/themes

   DisableRayfieldPrompts = false,
   DisableBuildWarnings = false, -- Prevents Rayfield from warning when the script has a version mismatch with the interface

   ConfigurationSaving = {
      Enabled = true,
      FolderName = rota, -- Create a custom folder for your hub/game
      FileName = "Rota"
   },

   Discord = {
      Enabled = true, -- Prompt the user to join your Discord server if their executor supports it
      Invite = "lyricsogoat", -- The Discord invite code, do not include discord.gg/. E.g. discord.gg/ ABCD would be ABCD
      RememberJoins = true -- Set this to false to make them join the discord every time they load it up
   },

   KeySystem = true, -- Set this to true to use our key system
   KeySettings = {
      Title = "Bota a key ai Bot",
      Subtitle = "Sistema de Chave",
      Note = "Acesso restrito.", -- Use this to tell the user how to get a key
      FileName = "Chave", -- It is recommended to use something unique as other scripts using Rayfield may overwrite your key file
      SaveKey = true, -- The user's key will be saved, but if you change the key, they will be unable to use your script
      GrabKeyFromSite = false, -- If this is true, set Key below to the RAW site you would like Rayfield to get the key from
      Key = {"bot"} -- List of keys that will be accepted by the system, can be RAW file links (pastebin, github etc) or simple strings ("hello","key22")
   }
})

Rayfield:Notify({
   Title = "Agradecimento",
   Content = "Testa meu script direito bot!",
   Duration = 6.5,
   Image = 4483362458,
})

local Tab = Window:CreateTab("Menu", 97682995973876) -- Title, Image
local Divider = Tab:CreateDivider()

local Paragraph = Tab:CreateParagraph({Title = "Atualizado e Funcionando!", Content = "_____________________________"})
local Divider = Tab:CreateDivider()

local Button = Tab:CreateButton({
   Name = "Vida Infinita ⛑️",
   Callback = function()
local carros = workspace:FindFirstChild("CarrosSpawnados")
if not carros then return end

for _, carro in ipairs(carros:GetChildren()) do
    local status = carro:FindFirstChild("Info")
        and carro.Info:FindFirstChild("Status")

    if status then
        -- Congelar Vida
        local vida = status:FindFirstChild("Vida")
        if vida and vida:IsA("NumberValue") then
            vida.Value = 99999
            vida:GetPropertyChangedSignal("Value"):Connect(function()
                vida.Value = 99999
            end)
        end

        -- Congelar Quebrou
        local quebrou = status:FindFirstChild("Quebrou")
        if quebrou and quebrou:IsA("BoolValue") then
            quebrou.Value = false
            quebrou:GetPropertyChangedSignal("Value"):Connect(function()
                quebrou.Value = false
            end)
        end
    end
end
   end,
})

local Button = Tab:CreateButton({
   Name = "Gasolina Infinita ⛽",
   Callback = function()
local carros = workspace:FindFirstChild("CarrosSpawnados")
if not carros then return end

for _, carro in ipairs(carros:GetChildren()) do
    local gasolina = carro:FindFirstChild("Info")
        and carro.Info:FindFirstChild("Status")
        and carro.Info.Status:FindFirstChild("Gasolina")
    
    if gasolina and gasolina:IsA("NumberValue") then
        gasolina.Value = 99999

        -- Congelar o valor
        gasolina:GetPropertyChangedSignal("Value"):Connect(function()
            gasolina.Value = 99999
        end)
    end
end
   end,
})
local Divider = Tab:CreateDivider()

local Button = Tab:CreateButton({
   Name = "▶️ Iniciar",
   Callback = function()
local vehiclePath
local primaryPart
local playerName = game.Players.LocalPlayer.Name

for _, vehicle in ipairs(workspace.CarrosSpawnados:GetChildren()) do
    local ownerInfoPath = vehicle:FindFirstChild("Info") and vehicle.Info:FindFirstChild("Owner")
    if ownerInfoPath and ownerInfoPath.Value == playerName then
        vehiclePath = vehicle
        break
    end
end

-- 🧹 Apagar rodas e água
if vehiclePath then
    -- Remove todas as rodas do veículo
    local wheelsFolder = vehiclePath:FindFirstChild("Wheels")
    if wheelsFolder then
        for _, wheel in ipairs(wheelsFolder:GetChildren()) do
            wheel:Destroy()
        end
    end

    -- Remove objeto de água
    local aguas = workspace:FindFirstChild("Complementos") and workspace.Complementos:FindFirstChild("Aguas")
    if aguas then
        aguas:Destroy()
    end

    -- Define PrimaryPart
    primaryPart = vehiclePath:FindFirstChild("Body") and vehiclePath.Body:FindFirstChild("Colisao") and vehiclePath.Body.Colisao:FindFirstChild("ColisaoPartNew")
    if primaryPart then
        vehiclePath.PrimaryPart = primaryPart
    end
end

_G.TeleportRunning = true
print("🚗 Iniciando sequência de teleportes...")

local function moveVehicleManually(destinationCFrame, speed)
    print("🔄 Movendo veículo para: ", destinationCFrame.Position)

    for _, part in ipairs(vehiclePath:GetDescendants()) do
        if part:IsA("BasePart") then
            part.Anchored = true
        end
    end

    local correctedCFrame = destinationCFrame + destinationCFrame.UpVector * 5
    local currentCFrame = vehiclePath:GetPrimaryPartCFrame()
    local distance = (correctedCFrame.Position - currentCFrame.Position).Magnitude
    local duration = distance / speed

    local startTime = tick()
    while tick() - startTime < duration and _G.TeleportRunning do
        local alpha = (tick() - startTime) / duration
        local newCFrame = currentCFrame:Lerp(correctedCFrame, alpha)
        vehiclePath:SetPrimaryPartCFrame(newCFrame)
        task.wait(0.016)
    end

    if _G.TeleportRunning then
        vehiclePath:SetPrimaryPartCFrame(correctedCFrame)
    end

    task.wait(1)

    for _, part in ipairs(vehiclePath:GetDescendants()) do
        if part:IsA("BasePart") then
            part.Anchored = false
        end
    end

    print("✅ Chegou ao destino.")
end

local function waitForProximityPrompt(targetCFrame)
    print("📡 Procurando ProximityPrompt...")
    for _, descendant in ipairs(workspace.Construcoes.LocaisRotas:GetDescendants()) do
        if descendant:IsA("ProximityPrompt") then
            local promptPart = descendant.Parent:IsA("BasePart") and descendant.Parent or nil
            if promptPart then
                local distance = (promptPart.Position - targetCFrame.Position).Magnitude
                if distance <= 30 then
                    print("⚡ Ativando prompt de interação.")
                    fireproximityprompt(descendant, 1)
                    task.wait(0.4)
                end
            end
        end
    end
end

local function pressKeyE()
    print("⌨️ Pressionando tecla E.")
    local success, errorMessage = pcall(function()
        local virtualInput = game:GetService("VirtualInputManager")
        virtualInput:SendKeyEvent(true, Enum.KeyCode.E, false, game)
        task.wait(0.1)
        virtualInput:SendKeyEvent(false, Enum.KeyCode.E, false, game)
    end)
end

local teleportLocations = {
    CFrame.new(4105.15137, 64.4651031, 482.882965, -0.113497302, 0.221895248, 0.968442559, -0.00584075926, 0.974575162, -0.223984882, -0.993521154, -0.0310781207, -0.109315619),
    CFrame.new(4219.3374, 74.9669495, 3166.12622, 0.431088239, -0.0753618702, -0.899157107, -0.0263625737, 0.995028675, -0.0960364044, 0.901924551, 0.065104261, 0.426958442),
    CFrame.new(1788.72302, 4.30489969, 2156.92188, 0.0213806406, 0.0225232076, 0.999517679, 0.000254597486, 0.999746025, -0.0225338005, -0.999771357, 0.000736261718, 0.0213694759),
    CFrame.new(361.122528, 30.455986, -841.140442, -0.581414104, 0.151928961, -0.799296737, -0.0179375876, 0.979777932, 0.199282452, 0.813410044, 0.130203083, -0.566931427),
    CFrame.new(569.097656, 4.65112209, 3193.24316, 0.99734354, 0.00110823882, 0.0728327483, 0.000802896393, 0.99965626, -0.0262055397, -0.0728367493, 0.0261944048, 0.9969998),
    CFrame.new(2763.05078, -2.09031916, 2740.63013, -0.103964806, -0.145493895, -0.983881533, -0.0144421114, 0.989358783, -0.14477779, 0.99447614, -0.000842468638, -0.104959734),
    CFrame.new(3206.22119, 4.58458328, 2260.93604, 0.00307788956, -0.0240117367, -0.999706924, 0.00170048152, 0.999710381, -0.0240065828, 0.999993801, -0.00162609352, 0.00311782956),
    CFrame.new(4223.43457, 4.63823986, 2415.22021, 0.0323573276, 0.021790944, 0.999238789, 0.00116096984, 0.999760807, -0.0218399223, -0.999475718, 0.00186676765, 0.032324288),
    CFrame.new(2879.26099, 4.58199167, 3181.17188, 0.999458849, -0.000999550801, -0.0328778066, 0.000256212894, 0.999744415, -0.022605557, 0.0328919962, 0.0225849003, 0.999203682),
    CFrame.new(3213.32446, 65.5097733, 671.534729, 0.137671217, -0.0245366283, -0.990173995, -0.000508352416, 0.999691248, -0.0248431452, 0.99047786, 0.00392354326, 0.137616232),
    CFrame.new(802.233093, 59.7998505, 2402.75073, -0.999849498, -0.00123223604, 0.0173058547, -0.00163992983, 0.999720991, -0.0235637482, -0.01727199, -0.0235885829, -0.999572515),
    CFrame.new(432.846771, 19.7031174, 889.635254, -0.993482113, 0.0684497654, 0.0911475942, 0.0579569377, 0.991883993, -0.113168553, -0.0981542021, -0.107148297, -0.989386141),
    CFrame.new(820.941711, 4.60487175, 2409.59863, -0.99926275, -0.000618405407, -0.0383865125, 0.00024763908, 0.999745607, -0.0225523133, 0.038390696, -0.0225451924, -0.999008417),
    CFrame.new(2343.80542, 4.30260563, 3768.93042, -0.99968797, -0.00050379266, 0.0249731895, -0.00107132399, 0.999741375, -0.0227174591, -0.0249552857, -0.0227371249, -0.999429941),
    CFrame.new(4037.37573, 13.8991241, -1730.19836, -0.0716072097, 0.0251205135, 0.997116506, 0.00814747252, 0.999664187, -0.0245995913, -0.997399628, 0.00636247126, -0.0717878342),
    CFrame.new(3458.49878, 22.6525593, 3671.08203, 0.999711454, -0.00077766797, -0.024009604, 0.000236469976, 0.999746025, -0.0225355122, 0.0240210313, 0.0225233324, 0.999457717)
}

coroutine.wrap(function()
    if not vehiclePath or not primaryPart then
        warn("❌ Veículo não identificado!")
        return
    end

    local cooldown = 17

    while _G.TeleportRunning do
        for i, targetCFrame in ipairs(teleportLocations) do
            if not _G.TeleportRunning then return end

            print(string.format("📍 Teleporte %d de %d", i, #teleportLocations))

            local currentCFrame = i == 1 and vehiclePath:GetPrimaryPartCFrame() or teleportLocations[i - 1]
            local distance = (targetCFrame.Position - currentCFrame.Position).Magnitude
            local speedFactor = 4
            local safeSpeed = math.min(distance / cooldown * speedFactor, 300)

            moveVehicleManually(targetCFrame, safeSpeed)
            task.wait(0.5)
            waitForProximityPrompt(targetCFrame)
            task.wait(0.5)

            if i == #teleportLocations then
                pressKeyE()
            end

            if i < #teleportLocations then
                print("⏳ Aguardando antes do próximo teleporte...")
                local waitStart = tick()
                while tick() - waitStart < 17 and _G.TeleportRunning do
                    task.wait(0.4)
                end
            end
        end

        print("🔁 Reiniciando sequência de teleportes em breve...")
        local waitStart = tick()
        while tick() - waitStart < 17 and _G.TeleportRunning do
            task.wait(0.4)
        end
    end

    print("🛑 Sequência encerrada.")
end)()
   end,
})

local Button = Tab:CreateButton({
   Name = "⏸ Parar",
   Callback = function()
_G.TeleportRunning = false
print("⏸️ Parado pelo usuário.")
   end,
})
local Divider = Tab:CreateDivider()

local Button = Tab:CreateButton({
   Name = " Fabricar ligeiro ",
   Callback = function()
for i,v in ipairs(game:GetService("Workspace"):GetDescendants()) do
 if v.ClassName == "ProximityPrompt" then
  v.HoldDuration = 0
 end
end
   end,
})
